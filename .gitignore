# Build output (make / g++)
/led
/virtual-strip
*.o
*.d

# Arduino build output (arduino-cli --export-binaries / local build dirs)
firmware/build/

# common daemon/firmware sources mirrored into the sketch by `make receiver`
firmware/src/

# Editor / IDE
.idea/
.vscode/
*.swp
*~

# OS cruft
.DS_Store
