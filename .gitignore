# Build output (make / g++)
/led
*.o
*.d

# Arduino build output (arduino-cli --export-binaries / local build dirs)
esp32_receiver/build/

# shared host/firmware sources mirrored into the sketch by `make receiver`
esp32_receiver/src/

# Editor / IDE
.idea/
.vscode/
*.swp
*~

# OS cruft
.DS_Store
