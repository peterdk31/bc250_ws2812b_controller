# Partition table for the receiver — a custom ESP-IDF table (selected via
# CONFIG_PARTITION_TABLE_CUSTOM in sdkconfig.defaults). A custom table is
# required because no built-in preset carves out a filesystem partition, and the
# firmware stores its power-on/shutdown recordings on LittleFS (mounted on the
# "storage" partition below — see main.cpp, esp_vfs_littlefs_register, which
# matches the partition by label, any subtype).
#
# Minimal 4 MB layout: a single factory app (no OTA — we always flash over
# USB/serial) and a 2 MB storage partition. nvs holds the receiver's saved baud
# and strip geometry.
# Name,     Type, SubType,  Offset,   Size,     Flags
nvs,        data, nvs,      0x9000,   0x7000,
factory,    app,  factory,  0x10000,  0x1F0000,
storage,    data, spiffs,   0x200000, 0x200000,
